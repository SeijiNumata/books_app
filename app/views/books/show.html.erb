<p id="notice"><%= notice %></p>

<p>
  <strong><%= t('description.title') %></strong>
  <%= @book.title %>
</p>

<p>
  <strong><%= t('description.memo') %></strong>
  <%= @book.memo %>
</p>

<p>
  <strong><%= t('description.author') %></strong>
  <%= @book.author %>
</p>

<p>
  <strong><%= t('description.picture') %></strong>
  <%= image_tag(@book.picture_url) if @book.picture.present? %>
</p>

<%= link_to t('button.edit'), edit_book_path(@book) %> |
<%= link_to t('button.back'), books_path %>
